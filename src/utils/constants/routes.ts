const routes = {
  trackers: "/",
  history: "/history",
  login: "/login",
  register: "/register",
};

const apiRoutes = {
  login: "/api/auth/login",
  session: "/api/auth/session",
  logout: "/api/auth/logout",
};

export { routes, apiRoutes };
